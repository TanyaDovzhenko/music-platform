# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthResponse {
  access_token: String!
  user: User!
}

input CreateUserInput {
  email: String!
  password: String!
  role: String!
}

type Mutation {
  signin(signInInput: SignInInput!): AuthResponse!
  signup(createUserInput: CreateUserInput!): AuthResponse!
}

type Query {
  currentUser: User!
  currentUserProfile: UserProfile!
  track(id: Int!): Track!
  tracks: [Track!]!
  user(id: Float!): User!
  userProfile(id: Int!): UserProfile!
  userProfiles: [UserProfile!]!
  userSingles(userProfileId: Int!): [Track!]!
  users: [User!]!
}

input SignInInput {
  email: String!
  password: String!
}

type Track {
  audio: String!
  id: Int!
  image: String
  name: String!
  rate: Int!
  userProfileId: Int
}

type User {
  email: String!
  id: Int!
  password: String
  role: UserRoles!
  userProfile: UserProfile
}

type UserProfile {
  avatar: String
  id: Int!
  name: String!
  status: String
  track: [Track!]
}

enum UserRoles {
  LISTENER
  MUSICIAN
}