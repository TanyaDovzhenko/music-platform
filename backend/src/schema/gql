# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

input AddTrackInput {
  playlistId: Int!
  trackId: Int!
}

type Album {
  authorName: String!
  authorUserId: Int!
  authorUserProfileId: Int!
  description: String
  id: Int!
  image: String
  name: String!
  rate: Float!
  tracks: [Track!]
  userProfiles: [UserProfile!]
}

type AuthResponse {
  access_token: String!
  user: User!
}

input CreatePlaylistInput {
  authorId: Int!
  description: String
  image: String
  isDefault: Boolean = false
  musicStylesIds: [Int!]
  name: String!
  tracksIds: [Int!]
}

input CreateUserInput {
  email: String!
  password: String!
  role: String!
}

type Mutation {
  addTrack(addTrackInput: AddTrackInput!): Boolean!
  createPlaylist(createPlaylistInput: CreatePlaylistInput!): Playlist!
  signin(signInInput: SignInInput!): AuthResponse!
  signup(createUserInput: CreateUserInput!): AuthResponse!
}

type Playlist {
  authorId: Int!
  description: String
  id: Int!
  image: String
  isDefault: Boolean!
  likes: Float!
  name: String!
  tracks: [Track!]
  userProfiles: [UserProfile!]
}

type Query {
  album(id: Int!): Album!
  currentUser: User!
  currentUserAlbums: [Album!]!
  currentUserPlaylists: [Playlist!]!
  currentUserProfile: UserProfile!
  firstImpTracks: Playlist!
  playlist(id: Int!): Playlist!
  singlesTracks(profileId: Int!): Playlist!
  track(id: Int!): Track!
  tracks: [Track!]!
  user(id: Float!): User!
  userAlbums(profileId: Int!): [Album!]!
  userProfile(id: Int!): UserProfile!
  userProfileByUserId(userId: Int!): UserProfile!
  userProfiles: [UserProfile!]!
  users: [User!]!
}

input SignInInput {
  email: String!
  password: String!
}

type Track {
  albumId: Int
  audio: String!
  id: Int!
  image: String
  name: String!
  rate: Int!
  userProfileId: Int
}

type User {
  email: String!
  id: Int!
  password: String
  role: UserRoles!
  userProfile: UserProfile
  userProfileId: Float!
}

type UserProfile {
  album: [Album!]
  albums: [Album!]
  avatar: String
  id: Int!
  name: String!
  playlists: [Playlist!]
  status: String
  track: [Track!]
}

enum UserRoles {
  LISTENER
  MUSICIAN
}